name: test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Test addwww
        run: |
          chmod +x addwww
          chmod +x test.bash
          ./test.bash
 

